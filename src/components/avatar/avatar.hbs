<div class="avatar">
    <img class="avatar__image avatar__image-{{ size }}" id="avatar" src="{{ image }}" alt="photo" />
    {{#if canUpdate}}
        <p class="avatar__image-text avatar__image-text-{{ size }}">Загрузить фото</p>
    {{/if}}
</div>
